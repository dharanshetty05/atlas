/**
 * Maximum allowed folder nesting depth in a workspace hierarchy.
 * Default: 20 levels per V1 architectural decision.
 */
export const MAX_FOLDER_DEPTH = 20;

/**
 * Maximum length of a folder name or document title.
 */
export const MAX_ENTITY_NAME_LENGTH = 255;
